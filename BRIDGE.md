# jikko のブリッジ v01

## 目的

このファイルは、ユーザの意図と実装判断をつなぐためのブリッジです。

agent はこのファイルを使って、次を行うこと。

- ユーザの依頼の奥にある本当の目的を推論する
- 曖昧または危険な実装前提に対して問い直す
- プロダクト判断と技術判断の整合を保つ
- 技術選定をユーザが納得できる形で説明する

## 言語ルール

- ユーザが読む文は日本語で書く
- agent 専用の内部メモだけ英語を許容する

## プロダクトの核

jikko は汎用的な Todo アプリではありません。

中核となる約束は次です。

- ユーザの認知負荷を最大限下げる
- ユーザに「今やるべきこと」を提示する
- なぜ今そのタスクをやる価値があるのかを説明する
- 実際に着手し、完了できる確率を高める
- 完了履歴を自信、報酬、長期的なリターンの実感へ変換する

## 現在のプロダクト前提

これらの前提は、以前の技術選定案よりも優先して扱うこと。

- 当面のユーザは 1 人だけである
- v1 では認証は不要である
- データは各ユーザがローカルで保持、管理する
- システムは認知負荷の最小化を最優先する
- タスクは実行可能な最小単位まで分割されるべきである
- タスク分解そのものがプロダクトループの一部であり、OODA の Observe に相当する

## 依頼から推論すべきこと

ユーザが機能を要求したとき、それを単純な CRUD 要求として扱わないこと。

必ず、その要求が次のどれに影響するかを確認すること。

- 優先順位決定の質
- 説明への信頼
- タスク着手のしやすさ
- タスク完了のしやすさ
- 迷いと意思決定疲れの削減
- 進捗と報酬の可視化による長期継続

## 問い直しの観点

依頼の粒度が粗い場合は、次の観点で実装対象を整理すること。

- この機能は、今やるべきことの判断に本当に役立つか
- 認知負荷を減らすか、それとも判断項目を増やすか
- ユーザが信頼できるだけの決定論性と説明可能性があるか
- 今実装すべきか、それとも中核ループ完成後に回すべきか
- プロダクト価値のための機能か、それとも単に作りやすいだけの機能か

中核ループを強化しない機能には、必要に応じて異議を唱えること。

## 守るべき中核ループ

最優先のループは次です。

1. タスクを入力または分解する
2. 意思決定に必要な構造化要因を見積もる
3. 今やるべきタスクを順位付けして選ぶ
4. 推奨理由を誠実かつ納得感のある形で説明する
5. 着手しやすい状態まで誘導する
6. 完了、報酬、結果を記録する
7. 進捗を自信と ROI としてユーザへ返す

新機能がこのループを弱めるなら、再検討すること。

## 技術方針

以前のスタック案は、単独利用かつローカルファーストという前提に合わせて調整すること。

現時点の優先方針は次です。

- アプリ基盤: `Next.js` + `TypeScript`
- v1 の保存先: ローカルファーストな DB。Hosted Postgres より `SQLite` を優先
- ORM / DB ツール: SQLite で素直に使えるなら `Prisma`。重いならより軽量な選択肢へ切り替える
- 認証: v1 では不要
- ランキング: 決定論的なルールベーススコアリング
- AI: 書き換え、言い換え、要約の補助に限定して使う
- 分析: まずはローカルイベント記録を優先し、Hosted analytics は v1 では必須としない
- 配置先: ローカルファースト前提のブラウザ UI

ただし、現在の正式な設計判断は [DESIGN.md](/Users/f42/dev/jikko.v/DESIGN.md:1) と [TECH_STACK.md](/Users/f42/dev/jikko.v/TECH_STACK.md:1) を優先すること。

## 推奨 v1 アーキテクチャ

小さなモジュラーモノリスを採用する。

モジュール例:

- `tasks`
- `scoring`
- `behavior`
- `history`
- `planning`

次の前提を持つ統合は早すぎるため避けること。

- 複数ユーザ
- クラウド同期
- 外部認証
- 重いバックグラウンド基盤

## データモデルの優先事項

データモデルは、単なるタスク保存ではなく、構造化されたスコアリングと行動支援を優先すること。

重要な task 属性:

- title
- description
- effort_estimate
- urgency
- impact
- penalty_of_delay
- momentum_gain
- emotional_resistance
- energy_required
- due_at
- status

あわせて次も保持すること。

- タスク分解の親子関係
- スコアのスナップショット
- 完了イベント
- 主観的な報酬や得られた結果
- 日次の推奨内容

## UX 指針

UI は、できる限りユーザに考えさせないことを目標にする。

全体に適用する UI ドグマは次です。

- 見えているのは、その瞬間に必要なものだけであること
- それ以外の情報は、適切な操作でいつでも確認できること
- 情報を常時並べるのではなく、必要になる瞬間まで隠すこと
- 1 画面で完結できる体験を優先し、スクロール前提をできる限り避けること

jikko にとって良い UX は次です。

- 今やるべき行動が 1 つ明確に示される
- なぜ重要なのかが見える
- タスク分解の摩擦が低い
- 完了記録の摩擦が低い
- 完了後に進捗実感が強く返ってくる

jikko にとって悪い UX は次です。

- 並び替え可能な一覧が多すぎる
- フィルタや設定が多すぎる
- 複雑さを早い段階で露出しすぎる
- 毎回ユーザ自身に優先順位を深く考えさせる

## 実装マップ

「どこをどういう風にいじると何が変わるか」の概要を以下に定義する。

### 画面を変えたいとき

- `src/app/screens/NowScreen.tsx`
  - 今やるべき 1 件の見せ方、理由の見せ方、開始/完了ボタンの導線が変わる
- `src/app/screens/InboxScreen.tsx`
  - タスク入力項目、分解前の収集体験、入力摩擦が変わる
- `src/app/screens/PlanScreen.tsx`
  - ランキング一覧の見せ方、優先順位の比較のさせ方が変わる
- `src/app/screens/HistoryScreen.tsx`
  - 完了履歴、ROI、進捗実感の返し方が変わる
- `src/app/screens/SettingsScreen.tsx`
  - ユーザに露出する調整項目の量と複雑さが変わる
- `src/app/components/*`
  - 共通 UI の印象、情報密度、操作感が横断的に変わる

### アプリ全体の導線を変えたいとき

- `src/app/Shell.tsx`
  - 画面の並び、全体レイアウト、画面の切り替え方が変わる
- `src/app/components/Sidebar.tsx`
  - ナビゲーション構造、ユーザに見せる情報の優先順位が変わる
- `src/app/components/Header.tsx`
  - アプリ全体で最上位に強調されるメッセージが変わる

### 優先順位ロジックを変えたいとき

- `src/domain/scoring/engine.ts`
  - `priority_score` の計算、`expected_roi` の考え方、`why_now_summary` の出し方が変わる
- `src/domain/scoring/defaults.ts`
  - 何をどれだけ重視するかという基本姿勢が変わる
- `src/domain/scoring/types.ts`
  - スコアリングで扱う構造そのものが変わる
- `src/domain/planning/select-next.ts`
  - ランキング上位の中から最終的に何を採用するかが変わる

優先順位ロジックをいじると、`Now`、`Plan`、`History` の意味が同時に変わると考えること。

### タスクの構造を変えたいとき

- `src/domain/tasks/types.ts`
  - タスクが持てる属性、バリデーション、入力要件が変わる
- `src/domain/tasks/factory.ts`
  - 新規作成時の初期値や生成ルールが変わる
- `src/app/screens/InboxScreen.tsx`
  - 実際にユーザが入力する項目とその順番が変わる

task 属性を変える場合は、スコアリングと DB スキーマも同時に見直すこと。

### 履歴や ROI の返し方を変えたいとき

- `src/domain/history/metrics.ts`
  - 完了数、自信、ROI などの集計ロジックが変わる
- `src/app/screens/HistoryScreen.tsx`
  - 集計結果をどう可視化するかが変わる
- `src/app/components/StatCard.tsx`
  - 指標の見せ方が横断的に変わる

### 状態管理を変えたいとき

- `src/state/task-store.ts`
  - タスク追加、開始、完了、ランキング再計算、履歴反映の挙動が変わる
- `src/state/app-store.ts`
  - 現在画面、ナビゲーション、画面状態管理が変わる

`task-store` は現在の実装上の中枢である。ここを変えると画面全体の挙動が変わる。

### DB を変えたいとき

- `src/infra/db/schema.ts`
  - SQLite 上の正式な保存構造が変わる
- `src/infra/db/client.ts`
  - フロントエンドから DB をどう呼ぶかが変わる
- `src/infra/db/repositories/*`
  - 永続化の API と取得単位が変わる
- `drizzle.config.ts`
  - Drizzle の生成対象と接続先が変わる

DB スキーマを変える場合は、対応する `domain/tasks`、`domain/scoring`、`state/task-store` も必ず追随させること。

### Tauri / ローカルアプリ挙動を変えたいとき

- `src-tauri/src/main.rs`
  - SQLite 初期化、Tauri command、ローカルホスト側の挙動が変わる
- `src-tauri/tauri.conf.json`
  - アプリ名、ウィンドウ設定、ビルド設定が変わる
- `src-tauri/capabilities/default.json`
  - Tauri 権限の範囲が変わる

### テストを変えたいとき

- `src/domain/scoring/engine.test.ts`
  - 優先順位ロジックの期待値が変わる
- `tests/e2e/app.spec.ts`
  - 実際の中核ループで何を保証するかが変わる
- `vite.config.ts`
  - Vitest の対象範囲や実行条件が変わる
- `playwright.config.ts`
  - E2E の実行条件が変わる

## 実装変更時の基本ルール

変更を加えるときは、次の対応関係を意識すること。

- 画面だけ変える:
  - `src/app/*` を中心に見る
- 入力項目を変える:
  - `src/domain/tasks/*` と `src/app/screens/InboxScreen.tsx` と `src/infra/db/schema.ts` を同時に見る
- 優先順位を変える:
  - `src/domain/scoring/*` と `src/domain/planning/*` と `src/state/task-store.ts` を同時に見る
- 永続化を進める:
  - `src/infra/db/*` と `src-tauri/src/main.rs` と `src/state/task-store.ts` を同時に見る
- 履歴や ROI を改善する:
  - `src/domain/history/*` と `src/app/screens/HistoryScreen.tsx` を同時に見る

局所的な修正に見えても、jikko では `入力 -> スコアリング -> 推奨 -> 完了 -> 履歴` がつながっているため、影響範囲を必ず横断確認すること。

## 技術判断をどう説明するか

実装判断を説明するときは、先にプロダクト上の意味から説明すること。

推奨する説明順序:

1. この判断がどのユーザ課題を解決するか
2. なぜ一見わかりやすい別案よりよいのか
3. どんな複雑さを避けられるのか
4. 何を後回しにできるのか

重要なのは技術的正しさだけでなく、ユーザが納得できることです。

## v1 で避けるもの

ユーザが明示的に求め、かつ強いプロダクト上の理由がない限り、次は避けること。

- 複数人コラボレーション
- SaaS 前提の構成
- 複雑な認証フロー
- 不透明な AI ベース優先順位付け
- モバイルネイティブ先行
- マイクロサービス
- 重い実験基盤

## 意思決定ルール

2 つの実装案がどちらも成立するなら、次をより満たす方を選ぶこと。

- 認知負荷をより減らせる
- より透明である
- ローカルで反復しやすい
- スコアリングロジックを検査しやすい
- 中核ループへの勢いを保てる
