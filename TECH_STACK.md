# jikko 技術要件定義書

## 1. 文書目的

本書は、jikko v1 の技術要件、採用技術、設計原則、実装範囲、および実装計画を定義するための正式文書である。

本書の目的は次のとおりとする。

- プロダクト要件と技術判断の整合を固定する
- 実装時の判断基準を明文化する
- 採用技術を比較検討ではなく正式決定として記録する
- 実装計画を段階的に規定し、開発の基準線とする

本書は、jikko v1 の要件定義書として扱う。

## 2. 適用対象

本書は、jikko v1 の設計、実装、検証、将来拡張の初期判断に適用する。

対象範囲は次とする。

- ローカルファーストな単独利用アプリケーション
- タスク分解、優先順位付け、着手支援、完了記録、履歴可視化
- 認知負荷を最小化する UI とロジック
- 説明可能な優先順位決定エンジン

対象外は本書の後半で明示する。

## 3. プロダクト前提

jikko v1 は、一般的な SaaS 型 Todo アプリケーションとは異なる前提で設計する。

前提条件を以下に定義する。

1. 利用者は当面 1 人である。
2. 認証機能は不要である。
3. データはローカルに保存し、利用者自身が管理する。
4. プロダクトの最重要価値は、認知負荷の削減である。
5. プロダクトの中核は、タスクの保存ではなく「今やるべきこと」の決定である。
6. タスクは実行可能な最小単位に分解されるべきである。
7. 優先順位決定は、説明可能かつ再現可能でなければならない。
8. AI は補助に使えても、中核判断の権限を持たない。

## 4. プロダクト要件

### 4.1 中核目的

jikko v1 は、次の状態を実現することを目的とする。

- 利用者が「何をやるべきか」を自力で毎回深く考えなくてよい
- 利用者が「なぜ今それをやるべきか」を納得できる
- 利用者が実際にタスクへ着手しやすくなる
- 利用者が完了を積み上げ、自信と ROI を可視化できる

### 4.2 中核ループ

v1 において保護すべき中核ループを以下に定義する。

1. タスクを追加する
2. タスクを実行可能な最小単位へ分解する
3. タスク評価に必要な構造化属性を付与する
4. 優先順位を算出し、今やるべき 1 件を選定する
5. 推奨理由を簡潔かつ納得可能な形で提示する
6. 着手を促進する
7. 完了を記録し、報酬感と進捗を返す
8. 履歴から ROI と成長実感を可視化する

以後の技術判断は、この中核ループを強化することを条件とする。

## 5. 技術方針

### 5.1 基本方針

jikko v1 は、`ローカルファーストなデスクトップアプリケーション` として実装する。

この判断の理由は次のとおりである。

- 単独利用、認証不要、ローカル保存という前提に最も整合する
- Web サービス前提の構成よりも実装複雑性を下げられる
- 中核である優先順位ロジックと UX に開発資源を集中できる
- 将来的な通知、バックアップ、ローカルファイル連携との相性がよい

### 5.2 採用技術

v1 における正式採用技術を以下に定義する。

- アプリ実行基盤: `Tauri`
- フロントエンド: `React` + `TypeScript` + `Vite`
- UI スタイリング: `Tailwind CSS`
- ローカルデータベース: `SQLite`
- DB レイヤー: `Drizzle ORM`
- バリデーション: `Zod`
- フォーム管理: `React Hook Form`
- 状態管理: `Zustand`
- グラフ描画: `Recharts`
- 単体/結合テスト: `Vitest`
- E2E テスト: `Playwright`
- 補助分析プロセス: `Python` 単発実行を許容

### 5.3 採用技術の役割定義

各採用技術の役割を以下に定義する。

- `Tauri`
  - ローカルアプリとしての配布、実行、OS 連携を担う
- `React` + `TypeScript` + `Vite`
  - UI 実装、コンポーネント構築、高速な開発サイクルを担う
- `SQLite`
  - タスク、履歴、スコア、設定の永続化を担う
- `Drizzle ORM`
  - スキーマ定義、DB 操作、マイグレーション管理を担う
- `Zod`
  - 入出力値、フォーム、内部データの検証を担う
- `React Hook Form`
  - タスク入力と分解入力のフォーム管理を担う
- `Zustand`
  - セッション状態および導出状態の管理を担う
- `Recharts`
  - ROI、完了推移、履歴可視化を担う
- `Python`
  - 分析専用の補助プロセスとして、履歴診断や将来の最適化計算を担う
- `Vitest`
  - 優先順位ロジックとドメインロジックの自動検証を担う
- `Playwright`
  - 中核ループの UI 動作検証を担う

## 6. 採用理由

### 6.1 Tauri を採用する理由

`Tauri` は、jikko v1 の前提に対して最も自然な実行基盤である。

採用理由:

- ローカルアプリとして完結しやすい
- SQLite を自然に扱える
- 認証を前提としない構成に適している
- ローカル通知、バックアップ、ファイル操作へ拡張しやすい
- Web 技術スタックを活かしつつ、SaaS 的な構成を回避できる

### 6.2 SQLite を採用する理由

`SQLite` は、単独利用とローカル保存の前提に最も適合する。

採用理由:

- セットアップが軽い
- 運用コストが低い
- ローカルバックアップと移行が容易である
- v1 のデータ量とアクセス特性に十分対応できる

### 6.3 Drizzle ORM を採用する理由

`Drizzle ORM` は、SQLite 中心の軽量構成と整合する。

採用理由:

- SQLite との親和性が高い
- 抽象化が過剰になりにくい
- スキーマと SQL の見通しを保ちやすい
- ローカルアプリに必要な軽量さを維持しやすい

### 6.4 React 系構成を採用する理由

`React` + `TypeScript` + `Vite` は、UI 実装速度と保守性のバランスが良い。

採用理由:

- 複雑な入力 UI と履歴可視化の両方に適している
- TypeScript によりドメインモデルの安全性を保ちやすい
- Vite によりローカル開発の反復速度を高く保てる

### 6.5 Python 補助プロセスを許容する理由

`Python` は常駐バックエンドとしてではなく、分析専用の補助プロセスとして限定的に採用可能とする。

採用理由:

- リアルタイム通信を前提にしなければ、複雑性を大きく増やさずに導入できる
- `pandas` や `numpy` を使った履歴分析の拡張余地を残せる
- 将来の ROI 最適化やタスクスケジューリング実験に接続しやすい
- 補助プロセスが不在でも、TypeScript 実装で機能継続できる

## 7. アーキテクチャ要件

### 7.1 全体構造

v1 は単一リポジトリの `モジュラーモノリス` として構成する。

### 7.2 必須モジュール

少なくとも以下のモジュールを持つこと。

- `app`
- `domain/tasks`
- `domain/scoring`
- `domain/behavior`
- `domain/history`
- `domain/planning`
- `infra/db`
- `infra/system`

### 7.3 各モジュールの責務

- `app`
  - 画面、レイアウト、UI コンポーネント、画面遷移
- `domain/tasks`
  - タスク、分解、依存関係、状態遷移
- `domain/scoring`
  - 優先順位計算、スコア内訳、説明文生成、ROI 推定
- `domain/behavior`
  - 着手支援、完了演出、報酬設計
- `domain/history`
  - 完了履歴、継続記録、振り返り素材
- `domain/planning`
  - 日次推奨、再評価、今やるべきことの選定
- `domain/analysis`
  - 履歴分析、見積もり差分診断、補助プロセスへの入力整形
- `infra/db`
  - スキーマ、Repository、マイグレーション、永続化
- `infra/system`
  - Tauri 連携、通知、入出力、システム統合、補助プロセス送受信

## 8. データ要件

### 8.1 永続化対象

少なくとも以下の情報を永続化対象とする。

- タスク本体
- タスク分解の親子関係
- スコアスナップショット
- 完了・開始・スキップ等のイベント
- 日次推奨結果
- 報酬記録
- 振り返り情報
- 利用者設定

### 8.2 主要テーブル要件

v1 では少なくとも次のテーブルを設けること。

- `tasks`
- `task_score_snapshots`
- `task_events`
- `daily_recommendations`
- `reward_records`
- `reflections`

### 8.3 task の必須属性

`tasks` には少なくとも以下の属性を持たせること。

- id
- title
- description
- status
- parent_task_id
- effort_estimate
- urgency
- impact
- penalty_of_delay
- momentum_gain
- emotional_resistance
- energy_required
- due_at
- created_at
- updated_at

## 9. 優先順位エンジン要件

### 9.1 基本要件

優先順位エンジンは、v1 の中核機能として以下を満たさなければならない。

- 決定論的であること
- 同一入力に対して再現可能であること
- スコア根拠を説明可能であること
- 利用者が納得しやすい入力要素を使うこと
- AI に最終判断を委ねないこと

### 9.2 入力要素

少なくとも以下を入力要素として扱うこと。

- urgency
- impact
- penalty_of_delay
- momentum_gain
- effort_estimate
- emotional_resistance
- energy_required
- due_at の近接度

### 9.3 出力要素

少なくとも以下を出力すること。

- `priority_score`
- `expected_roi`
- `why_now_summary`

### 9.4 説明要件

説明文は自由生成のブラックボックスではなく、スコア内訳に基づいて構成すること。

説明には少なくとも次の観点を含めること。

- 今やる利益
- 今やらない損失
- 着手しやすさ、または着手障壁

## 10. AI 利用要件

### 10.1 AI の位置づけ

AI は v1 において補助機能であり、中核判断機能ではない。

### 10.2 許可される用途

AI は次の用途に限り使用可能とする。

- タスク文言の言い換え
- タスク分解候補の補助提示
- 完了後の振り返り文案生成
- 説明文の自然な表現調整

### 10.3 禁止用途

AI を次の用途に使用してはならない。

- 優先順位の最終決定
- スコアのブラックボックス算出
- 根拠のない動機づけ演出のみを行うこと

## 11. UI/UX 要件

### 11.1 基本要件

UI/UX は、認知負荷の最小化を最上位要件として設計する。

### 11.2 主要画面

v1 では少なくとも次の画面を実装すること。

1. `Now`
2. `Inbox`
3. `Plan`
4. `History`
5. `Settings`

### 11.3 Now 画面要件

`Now` 画面は最重要画面とし、少なくとも次を満たすこと。

- 今やるべきタスクを 1 件、明確に提示する
- 推奨理由を短く提示する
- 着手操作を明確にする
- 完了後に次の行動へ遷移しやすくする

### 11.4 禁止事項

次の UI は v1 では避けること。

- 設定過多
- フィルタ過多
- 並び替え機能への過剰依存
- 利用者が毎回優先順位を手動で判断する前提

## 12. 状態管理要件

状態は次の 3 層に分離すること。

1. 永続状態
2. セッション状態
3. 導出状態

要件:

- 永続状態の唯一の真実は SQLite に置く
- セッション状態と導出状態は `Zustand` で管理する
- スコアや推奨結果は必要に応じて再計算可能であること

## 13. 非機能要件

v1 は少なくとも次を満たすこと。

- オフラインで完全動作する
- 起動が速い
- ローカルバックアップが容易である
- スキーマ変更に追従可能である
- スコア計算が再現可能である
- 画面遷移よりも即応性を優先する

## 14. テスト要件

### 14.1 単体/結合テスト

`Vitest` を用い、少なくとも次を検証対象とする。

- 優先順位計算
- スコア内訳生成
- タスク状態遷移
- ROI 算出

### 14.2 E2E テスト

`Playwright` を用い、少なくとも次を検証対象とする。

- タスク追加
- タスク分解
- 今やるべきタスクの提示
- 着手
- 完了記録
- 履歴反映

## 15. 実装計画

### Phase 1: 基盤構築

実装対象:

- `Tauri` プロジェクト初期化
- `React` + `TypeScript` + `Vite` 構成
- `Tailwind CSS` 導入
- `SQLite` + `Drizzle ORM` 初期化
- 基本ディレクトリ構成の作成

完了条件:

- ローカルでアプリが起動すること
- DB 接続とマイグレーションが動作すること

### Phase 2: 中核データ構築

実装対象:

- `tasks`
- `task_events`
- `task_score_snapshots`
- 基本 CRUD
- タスク分解機能

完了条件:

- タスク追加、更新、分解、保存が行えること

### Phase 3: 優先順位エンジン構築

実装対象:

- スコア計算関数
- スコア内訳
- `why_now_summary` 生成
- `Now` 画面の初版

完了条件:

- 今やるべき 1 件が算出され、理由とともに表示されること

### Phase 4: 行動支援機能構築

実装対象:

- 着手操作
- 開始/完了イベント記録
- 完了時の報酬フィードバック
- 次アクションへの接続

完了条件:

- タスクを開始し、完了し、結果が履歴へ反映されること

### Phase 5: 履歴・ROI 可視化

実装対象:

- `History` 画面
- ストリーク表示
- 完了数推移
- ROI 可視化

完了条件:

- 過去の行動が視覚的に確認できること
- 自信やリターンの蓄積が見えること

### Phase 6: 補助機能

実装対象:

- ローカル通知
- バックアップ/エクスポート
- 補助的な AI 機能

完了条件:

- 中核ループを壊さずに補助機能が追加されていること

## 16. 非採用範囲

v1 では以下を実装対象外とする。

- 複数ユーザ対応
- アカウント管理
- クラウド同期
- SaaS 前提の運用
- モバイルネイティブアプリ
- リアルタイム共同編集
- ブラックボックスな AI 優先順位決定
- 重い通知ルールエンジン
- マイクロサービス分割

## 17. 将来拡張指針

v1 完了後、次の条件が満たされた場合に限り拡張を再検討する。

- 優先順位提示が実際に信頼されている
- タスク分解が着手率改善に寄与している
- 完了報酬と履歴可視化が継続に寄与している

将来拡張候補:

- クラウド同期
- Web 版配布
- 複数端末対応
- AI 補助機能の強化

ただし、いずれの拡張も v1 の中核ループを損なわないことを条件とする。

## 18. 最終決定

jikko v1 の技術構成は、以下を正式採用とする。

- 実行基盤: `Tauri`
- UI: `React` + `TypeScript` + `Vite` + `Tailwind CSS`
- DB: `SQLite`
- DB レイヤー: `Drizzle ORM`
- バリデーション: `Zod`
- フォーム: `React Hook Form`
- 状態管理: `Zustand`
- グラフ: `Recharts`
- テスト: `Vitest` + `Playwright`
- 優先順位エンジン: 決定論的ルールベース
- AI: 補助用途のみ

以上を、jikko v1 における正式な技術要件および実装計画として採用する。
