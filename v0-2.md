# issues
- もっと数字をメインにしたい
   - ROIとか各種指数がバンとでて、逃げようがないようにしたい
- OODAループをメイン画面にしたい
  - カーソル(原義の)みたいなのでOODAのスライディングウィンドウみたいなUIイメージ
- 画面の没入感のために、中央上にタスク名、中央下に指数表示、ほとんど同じ大きさで、それ以外はヘッダかフッタで統一的な、情報量がよく管理された画面にしたい
- Quireみたいな感じで、自由で保存の効くタスク階層構造を実現したい。
- 本筋には必要ないが、ical出力が簡単であれば実装したい

# v0.2 implementation notes
- v0.2 の主役は UI 再設計
  - DB 本接続より先に、Now 画面の体験を固める
  - 中央上にタスク名、中央下に `ROI` と `優先度` を同格で大きく置く
- OODA は横スライド型の状態表示に留める
  - `Observe / Orient / Decide / Act` を 1 本の流れとして見せる
  - v0.2 では OODA の専用入力や専用保存は持たない
- `Plan` は主役画面ではなく補助画面
  - `Now` を侵食しない
  - なぜ次点なのかを短く比較できる構成にする
- `Inbox` は入力摩擦を上げない
  - タイトルと判断材料を短く入れられることを優先する
  - 将来の階層化を壊さないが、v0.2 では親子 UI は入れない

# task tree memo for v0.3+
- `parentTaskId` は将来の自由ツリーの足場として維持する
- v0.2 では次を未実装として明示する
  - 自由編集 UI
  - 再帰ツリー表示
  - 親子スコア連動
- 次段階で決めるべきこと
  - スコアリング対象は葉だけにするか、親も含めるか
  - `Now` 推奨対象は親タスクか、実行可能な末端タスクか
  - 子が完了したとき、親の進行中・完了をどう判定するか

# ical memo
- v0.2 の必須要件には含めない
- 余力が出た場合のみ、単発 `.ics` エクスポートを最小仕様で検討する
- 双方向同期や継続更新は対象外
